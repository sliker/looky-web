/* import { Action } from '@ngrx/store';
import { type } from '../../util';

import {
    initialConfig
} from './config.action-types';

export class InitialConfigAction implements Action {
    type = initialConfig;

    constructor(public payload: Object) { }
}

export type Actions = InitialConfigAction;
*/
